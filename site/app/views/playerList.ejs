<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link rel="stylesheet" href="/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/fontawesome-all.css">
  </head>
  <body>
    <div class="container">
      <% include ./partials/nav %>
      <%  let player = {}; %>
        <a class="btn btn-primary" href="/squadOpt/player/add">Add</a><br/><br/>
         <% if (players.length == 0) { %>

           <h4> No players to display. Add one above.</h4>
         <%} else {%>
          <% for(let i = 0; i < players.length; i++) {
              player = players[i] %>
              <% include ./partials/playerCard %>
          <% } %>
        <% } %>
      <p class="font-weight-light font-italic">Served by host: <%= hostname %></p>
    </div> <!-- container -->
  </body>
  <script src="/js/jquery.min.js"></script>
  <script src="/js/bootstrap.min.js"></script>
</html>
